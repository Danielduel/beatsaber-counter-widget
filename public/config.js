window.BeatSaberCounterWidget = window.BeatSaberCounterWidget || {
  /**
   * @param {number} notesCut
   * @returns {string}
   */
  notesCutText(notesCut) {
    return `Bloqs booped ${notesCut}`;
  }
};
